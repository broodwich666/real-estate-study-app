import Foundation

enum AppConfig {
    // This is the single hosted source used by Web, iOS, and the new macOS wrapper.
    static let appURL = URL(string: "https://broodwich666.github.io/real-estate-study-app/")!
}
